const test = require('tape')

const alt = require('./alt')
const ap = require('./ap')
const bimap = require('./bimap')
const both = require('./both')
const chain = require('./chain')
const coalesce = require('./coalesce')
const compareWith = require('./compareWith')
const concat = require('./concat')
const cons = require('./cons')
const contramap = require('./contramap')
const either = require('./either')
const empty = require('./empty')
const equals = require('./equals')
const evalWith = require('../State/evalWith')
const execWith = require('../State/execWith')
const extend = require('./extend')
const filter = require('./filter')
const first = require('./first')
const fold = require('./fold')
const fst = require('../Pair/fst')
const head = require('./head')
const log = require('../Writer/log')
const map = require('./map')
const merge = require('../Pair/merge')
const option = require('./option')
const promap = require('./promap')
const read = require('../Writer/read')
const reduce = require('./reduce')
const reduceRight = require('./reduceRight')
const reject = require('./reject')
const run = require('./run')
const runWith = require('./runWith')
const second = require('./second')
const sequence = require('./sequence')
const snd = require('../Pair/snd')
const swap = require('./swap')
const tail = require('./tail')
const traverse = require('./traverse')
const valueOf = require('./valueOf')


test('pointfree entry', t => {
    t.equal(alt, alt, 'provides the alt pointfree')
    t.equal(ap, ap, 'provides the ap pointfree')
    t.equal(bimap, bimap, 'provides the bimap pointfree')
    t.equal(both, both, 'provides the both pointfree')
    t.equal(chain, chain, 'provides the chain pointfree')
    t.equal(compareWith, compareWith, 'provides the compareWith pointfree')
    t.equal(coalesce, coalesce, 'provides the coalesce pointfree')
    t.equal(concat, concat, 'provides the concat pointfree')
    t.equal(cons, cons, 'provides the cons pointfree')
    t.equal(contramap, contramap, 'provides the contramap pointfree')
    t.equal(either, either, 'provides the either pointfree')
    t.equal(empty, empty, 'provides the empty pointfree')
    t.equal(equals, equals, 'provides the equals pointfree')
    t.equal(evalWith, evalWith, 'provides the evalWith pointfree')
    t.equal(execWith, execWith, 'provides the execWith pointfree')
    t.equal(extend, extend, 'provides the extend pointfree')
    t.equal(filter, filter, 'provides the filter pointfree')
    t.equal(first, first, 'provides the first pointfree')
    t.equal(fold, fold, 'provides the fold pointfree')
    t.equal(fst, fst, 'provides the fst pointfree')
    t.equal(head, head, 'provides the head pointfree')
    t.equal(log, log, 'provides the log pointfree')
    t.equal(map, map, 'provides the map pointfree')
    t.equal(merge, merge, 'provides the merge pointfree')
    t.equal(option, option, 'provides the option pointfree')
    t.equal(promap, promap, 'provides the promap pointfree')
    t.equal(read, read, 'provides the read pointfree')
    t.equal(reduce, reduce, 'provides the reduce pointfree')
    t.equal(reduceRight, reduceRight, 'provides the reduceRight pointfree')
    t.equal(reject, reject, 'provides the reject pointfree')
    t.equal(run, run, 'provides the run pointfree')
    t.equal(runWith, runWith, 'provides the runWith pointfree')
    t.equal(second, second, 'provides the second pointfree')
    t.equal(sequence, sequence, 'provides the sequence pointfree')
    t.equal(snd, snd, 'provides the snd pointfree')
    t.equal(swap, swap, 'provides the swap pointfree')
    t.equal(tail, tail, 'provides the tail pointfree')
    t.equal(traverse, traverse, 'provides the traverse pointfree')
    t.equal(valueOf, valueOf, 'provides the valueOf pointfree')

    t.end()
})  
